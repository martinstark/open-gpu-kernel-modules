--- a/src/common/displayport/src/dp_connectorimpl.cpp
+++ b/src/common/displayport/src/dp_connectorimpl.cpp
@@ -3863,11 +3863,19 @@
         return;
     }
 
-    // Skip gating modeset on HPD for DDS panels
+    // An unplugged sink still needs detach bookkeeping after head shutdown.
     if(!previousPlugged && !bClientForcedConnected && !main->isInternalPanelDynamicMuxCapable())
     {
-        DP_ASSERT(0 && "DPCONN> dpPreModeset called when Plugged State is false!");
-        return;
+        for (NvU32 i = 0; i < NV_MAX_HEADS; i++)
+        {
+            if ((params.headMask & NVBIT(i)) && params.head[i].pTarget != NULL)
+            {
+                DP_ASSERT(0 && "DPCONN> dpPreModeset attach called when Plugged State is false!");
+                return;
+            }
+        }
+        if (params.headMask)
+            DP_PRINTF(DP_NOTICE, "DPFIX allowing disconnected detach headMask=0x%x", params.headMask);
     }
 
     this->bFECEnable |= this->needToEnableFEC(params);
--- a/kernel-open/nvidia-modeset/nvidia-modeset-linux.c
+++ b/kernel-open/nvidia-modeset/nvidia-modeset-linux.c
@@ -143,6 +143,10 @@
 static bool dp_recovery_diag = true;
 module_param_named(dp_recovery_diag, dp_recovery_diag, bool, 0444);
 
+MODULE_PARM_DESC(dp_disconnected_detach_fix, "Disconnected DP detach test build marker");
+static bool dp_disconnected_detach_fix = true;
+module_param_named(dp_disconnected_detach_fix, dp_disconnected_detach_fix, bool, 0444);
+
 #if IS_ENABLED(CONFIG_EXTCON_DISP_CABLE_STATE)
 /*
  * Extcon reporting for HDMI video and audio state.
